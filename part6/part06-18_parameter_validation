# Write your solution here
def new_person(name: str, age: int):
    if not name or 2 > len(name.split(" ")) or len(name) > 40 or age < 0 or age > 150:
        raise ValueError("Somthing wrong")
    return name, age


if __name__ == "__main__":
    new_person('James Jameson', 32)
